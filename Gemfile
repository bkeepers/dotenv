source 'https://rubygems.org'
gemspec

# FIXME: delete this; it's only here for initial peer review
group :development do
  gem 'foreman-env', :git => 'git://github.com/toolbear/foreman-env.git', :ref => 'dotenv-extensions'
end